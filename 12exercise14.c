int temperatures[7][24];
bool search(const int a[][], int n, int key);
{
    int *p_day, *p_hour, key = 32;
    for(p_day=a; p_day < (a[7][]; p_day++)
        for ()
}
/*Scratch this, did it all wrong, search is apparently a prebuilt function
in C used for arrays, I will plug william's program here for reference.
temperature is the array, 7 is the days, 24 is the hours, seperated by an
asterisk which i guess is legal, 32 is the key.*/
bool has32 = search(temperatures, 7 * 24, 32);